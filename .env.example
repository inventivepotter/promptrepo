# Application Configuration

## Hosting Type
# Options: individual, organization, multi-tenant
# - individual: Single user, no auth required, LLM configs only
# - organization: Multi-user with GitHub OAuth, requires LLM configs
# - multi-tenant: Multiple organizations, no global LLM configs
HOSTING_TYPE="individual"

## GitHub OAuth (Required for 'organization' hosting type only)
# Create a GitHub OAuth App: https://github.com/settings/applications/new
# Set Authorization callback URL to: http://localhost:3000/auth/callback/github
GITHUB_CLIENT_ID="your_github_client_id"
GITHUB_CLIENT_SECRET="your_github_client_secret"

## LLM Configs (Not used for 'multi-tenant' hosting type)
# JSON array of LLM provider configurations
# Each config should have: provider, model, apiKey, and optionally apiBaseUrl
# Examples include OpenAI, Anthropic, and local LM Studio instances
LLM_CONFIGS='[{"provider":"openai","model":"gpt-4","apiKey":"your_openai_api_key"},{"provider":"anthropic","model":"claude-3-sonnet","apiKey":"your_anthropic_api_key"},{"provider":"lmstudio","model":"your-local-model","apiKey":"not-needed","apiBaseUrl":"http://host.docker.internal:1234/v1"}]'

## Admin Emails (Required for 'organization' and 'multi-tenant' hosting types)
# JSON array of admin email addresses who can modify configurations
ADMIN_EMAILS='["admin@example.com"]'

# Environment
ENVIRONMENT="development"

# Backend URL for direct API calls
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Frontend Port
PORT="3000"

# Database
DATABASE_URL="sqlite:////persistence/database/promptrepo.db"