# Application Configuration

## Hosting Type
# Options: individual, organization
# - individual: Single user, no auth required, LLM configs only
# - organization: Multi-user with GitHub OAuth, requires LLM configs
HOSTING_TYPE="individual"

SESSION_COOKIE_SECRET="your-very-secure-and-random-secret-key"

## GitHub OAuth (Required for 'organization' hosting type only)
# Create a GitHub OAuth App: https://github.com/settings/applications/new
# Set Authorization callback URL to: http://localhost:3000/auth/callback/github
OAUTH_CONFIGS='[{"provider": "github", "client_id": "your_github_client_id", "client_secret": "your_github_client_secret", "redirect_url": "http://localhost:8080/auth/callback/github"}]'


## LLM Configs
# JSON array of LLM provider configurations
# Each config should have: provider, model, api_key, and optionally api_base_url
# Examples include OpenAI, Anthropic, and local LM Studio instances
DEFAULT_LLM_CONFIGS='[{"id":"1","provider":"openai","model":"gpt-4","api_key":"your_openai_api_key"},{"id":"2","provider":"anthropic","model":"claude-3-sonnet","api_key":"your_anthropic_api_key"},{"id":"3","provider":"lmstudio","model":"your-local-model","api_key":"not-needed","api_base_url":"http://host.docker.internal:1234/v1"}]'

# Environment
ENVIRONMENT="development"

# Backend URL for direct API calls
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Frontend Port
PORT="3000"

# Database
DATABASE_URL="sqlite:////persistence/database/promptrepo.db"